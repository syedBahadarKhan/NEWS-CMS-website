 <% if(errors && errors.length > 0) { %>
        <div class="alert alert-danger mt-2">
        <% errors.forEach(error => { %>
            <ul>
            <li><%= error.msg %></li>
            </ul>
            
        <% }); %>
        </div>
     <% } %>